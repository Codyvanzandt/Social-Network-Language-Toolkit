class EnterExitNotation:
    def __init__(self, edge_data, new_entrants_only=True):
        self.edges = self.get_edges(edge_data)
        self.on_stage = set()

    def get_edges(self, edge_data):
        for edge_group in edge_data:
            pass

    def generate_edge_list(self, source_and_target_data):
        pass